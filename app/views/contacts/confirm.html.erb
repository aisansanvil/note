<div class="container">
  <div class="col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <h3>この内容で送信しますか？</h3><br />


    <%= form_for(@contact) do |f| %>
        <%= f.hidden_field :name %>
        <%= f.hidden_field :email %>
        <%= f.hidden_field :content %>

        <p>お名前:<%= @contact.name %></p><br />
        <p>メールアドレス:<%= @contact.email %></p><br />
        <p>お問い合わせ内容:<%= @contact.content %></p><br />

        <%= f.submit "送信", class: 'btn btn-primary btn-block' %>
    <% end %>

    <%= form_for(@contact, url: new_contact_path, method: 'get') do |f| %>
        <%= f.hidden_field :name %>
        <%= f.hidden_field :email %>
        <%= f.hidden_field :content %>
        <%= f.submit "もどる", name: 'back', class: 'btn btn-default btn-block' %>
    <% end %>
  </div>
</div>
